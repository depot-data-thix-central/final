// lib/services/training_service.dart

/// Module de formation non utilisé dans la version actuelle de THIX ID.
/// Conservé sous forme de classe vide pour éviter de casser les injections de dépendances (Providers).
class TrainingService {
  
  TrainingService({dynamic client});

}
